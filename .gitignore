node_modules/
.next/
.env
*.db
*.db-*
# Nur das Laufzeit-Medienverzeichnis am Projektstamm ignorieren
# (NICHT legacy/media — das sind die Seed-Bilder und müssen ins Repo).
/media/
dist/
.DS_Store
*.tsbuildinfo
next-env.d.ts
# Secret-Dateien (z. B. Ticket-API-Token), per Docker-Volume eingebunden
/secrets/
